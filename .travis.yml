language: python
dist: xenial
python:
  - "3.7"
install:
  - pip install poetry twine
script:
  - poetry install; poetry build; twine check dist/*

# matrix:
#   include:
#     - os: linux
#       dist: xenial
#     - os: osx
#       osx_image: xcode7.2